/** React. */
import { Fragment } from 'react';

/** Component. */
import Summary from '../../ui/summary';

const Home = () => {
    return (
        <Fragment>
            <Summary />
            <h1>Home explorer page.</h1>
        </Fragment>
    );
}

export default Home;