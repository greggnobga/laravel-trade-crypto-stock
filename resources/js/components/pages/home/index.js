/** React. */
import { Fragment } from 'react';

/** Component. */
import Summary from '../../ui/summary';

const Home = () => {
    return (
        <div id="wrapper">
            <Summary />
            <h1>Home explorer page.</h1>
        </div>
    );
}

export default Home;